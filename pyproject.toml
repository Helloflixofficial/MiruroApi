[project]
name = "miruro-api"
version = "0.1.0"
description = "Miruro API"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "httpx",
    "uvicorn",
    "python-dotenv",
    "curl_cffi==0.7.4",
]

[tool.vercel]
entrypoint = "api:app"
